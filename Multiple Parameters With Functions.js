/*
you can define multiple parameters by separating them with commas
they are used within the functions definitions
provide arguments in the same order you defined them


function myFunction(x,y)
{
    some code
}

*/

function test()
{
    if(x>y)
    {
        document.write(x)
    } 
    else
    {
        document.write(y)
    }   
}

test(5,8)
//output is 8